# Configuration for the ACE Framework

# Language Model Configuration
language_model:
  name: "mock"  # Can be "mock" or "openai"

  openai:
    api_key: "YOUR_OPENAI_API_KEY"

  mock:
    responses:
      - '[{"content": "Mock insight 1", "metadata": {"source": "reflector", "type": "mock"}}, {"content": "Mock insight 2", "metadata": {"source": "reflector", "type": "mock"}}]'

# Settings for the CLI
cli_settings:
  default_task: "Default task from config"
